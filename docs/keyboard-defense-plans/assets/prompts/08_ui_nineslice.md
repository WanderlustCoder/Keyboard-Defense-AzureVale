OPTIONAL TASK: Implement UI 9-slice assets and wiring.

DELIVERABLES:
1) Extend the SVG generator to create UI sprites:
   - ui_panel_bg, ui_panel_header, ui_button, ui_prompt_bg, ui_threat_card_bg
2) Ensure they are designed for 9-slice scaling:
   - corners remain crisp, edges repeat cleanly
3) Add integration in UI scenes using `NinePatchRect` or `StyleBoxTexture`.
4) Add a visual demo scene that shows:
   - all UI panels at multiple sizes
   - icons rendered at 16x16 and 24x24

LANDMARKS in final response (mandatory).
